const WorkoutItem = () => {
  return <div className="bg-blue-100 w-full">WorkoutItem</div>;
};

export default WorkoutItem;

// Displays a single workout (date, exercise, weight, reps).
// Includes delete button.
